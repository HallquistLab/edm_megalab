---
interface Props {
  eyebrow: string;
  title: string;
  intro: string;
}

const { eyebrow, title, intro } = Astro.props;
---

<header class="page-intro shell">
  <p class="eyebrow">{eyebrow}</p>
  <div>
    <h1>{title}</h1>
    <p>{intro}</p>
  </div>
</header>

---
import "../styles/global.css";

interface Props {
  title?: string;
  description?: string;
  socialImage?: string | null;
  pageType?: "website" | "article";
}

const {
  title = "Emory Decision-Making Megalab",
  description = "A biweekly forum for colleagues across Emory to share works in progress, practical methods, and decision-making research.",
  socialImage = "https://hallquistlab.github.io/edm_megalab/og.png",
  pageType = "website",
} = Astro.props;
const base = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
const pageTitle =
  title === "Emory Decision-Making Megalab"
    ? title
    : `${title} · Emory Decision-Making Megalab`;
const canonicalUrl = new URL(Astro.url.pathname, Astro.site);
const navItems = [
  { label: "Schedule", path: "schedule/", matches: ["schedule/", "meetings/"] },
  { label: "Article queue", path: "articles/", matches: ["articles/"] },
  { label: "Reading room", path: "readings/", matches: ["readings/"] },
  { label: "Materials", path: "materials/", matches: ["materials/"] },
  { label: "About us", path: "about/", matches: ["about/"] },
];
const isCurrent = (matches: string[]) =>
  matches.some((path) => {
    const target = `${base}${path}`;
    return Astro.url.pathname === target || Astro.url.pathname.startsWith(target);
  });
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#012169" />
    <link rel="canonical" href={canonicalUrl} />
    <meta property="og:type" content={pageType} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    {
      socialImage && (
        <>
          <meta property="og:image" content={socialImage} />
          <meta
            property="og:image:alt"
            content="EDM Megalab — Emory Decision-Making Megalab"
          />
        </>
      )
    }
    <meta name="twitter:card" content={socialImage ? "summary_large_image" : "summary"} />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    {socialImage && <meta name="twitter:image" content={socialImage} />}
    <title>{pageTitle}</title>
  </head>
  <body>
    <a class="skip-link" href="#main-content">Skip to main content</a>
    <header class="site-header">
      <a class="wordmark" href={base} aria-label="Emory Decision-Making Megalab home">
        <span class="wordmark-mark" aria-hidden="true">EDM</span>
        <span>Emory Decision-Making<br />Megalab</span>
      </a>
      <nav class="desktop-nav" aria-label="Main navigation">
        {
          navItems.map((item) => (
            <a
              href={`${base}${item.path}`}
              aria-current={isCurrent(item.matches) ? "page" : undefined}
            >
              {item.label}
            </a>
          ))
        }
      </nav>
      <a class="header-action" href={`${base}propose/`}>
        Propose a Session <span aria-hidden="true">→</span>
      </a>
      <details class="mobile-menu">
        <summary>Menu</summary>
        <nav aria-label="Mobile navigation">
          {
            navItems.map((item) => (
              <a
                href={`${base}${item.path}`}
                aria-current={isCurrent(item.matches) ? "page" : undefined}
              >
                {item.label}
              </a>
            ))
          }
          <a href={`${base}propose/`}>
            Propose a Session <span aria-hidden="true">→</span>
          </a>
        </nav>
      </details>
    </header>
    <main id="main-content">
      <slot />
    </main>
    <div
      class="action-feedback"
      data-action-feedback
      role="status"
      aria-live="polite"
      aria-atomic="true"
    >
    </div>
    <footer>
      <div>
        <p>Emory University</p>
      </div>
      <nav class="footer-nav" aria-label="Footer navigation">
        {navItems.map((item) => <a href={`${base}${item.path}`}>{item.label}</a>)}
        <a href={`${base}admin/`}>Coordinator console</a>
      </nav>
    </footer>
  </body>
</html>
